<%= form_for(item) do |f| %>
  <% if item.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(item.errors.count, "error") %> prohibited this item from being saved:</h2>

      <ul>
      <% item.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

    <div class="field">
      <%= f.label :category_id %>
      <%#= f.text_field :category_id %>
      <%= f.collection_select :category_id, Category.all, :id, :category_name, prompt: true %>
    </div>

  <div class="field">
    <%= f.label :item_type %>
    <%= f.text_field :item_type %>
  </div>

  <div class="field">
    <%= f.label :description %>
    <%= f.text_area :description %>
  </div>

  <div class="field">
    <%= f.label :show_image %>
    <%= f.file_field :show_image %>
  </div>

  <div class="field">
    <%= f.label :cost %>
    <%= f.text_field :cost %>
  </div>

  <div class="field">
    <%= f.label :cost_durations %>
    <%= f.text_field :cost_durations %>
  </div>

  <div class="field">
    <%= f.label :attainability %>
    <%= f.check_box :attainability, {checked: true} %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
