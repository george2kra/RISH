<div class='wrapper_top'>Welcome to R.I.S.H.</div>
<ul>
  <li>Rental of Items for Services within your Household </li>
  <li>Save money by renting</li>
  <li>Hassle-free rental from anywhere</li>
  <li>Got stuff to rent? Then rent them out and make money</li>
  <li>Track and Manage your stuff with ease</li>
</ul>

<div class='wrapper_bottom'>
  <a href="/categories" class="book_button">Look at what you can rent</a><br><br>
</div>

<% if (user_signed_in? && Item.exists?(user_id: current_user.id) )%>
  <%= form_tag items_path, method: "get" do %>
    <a href="/items/new" class="book_button">Add your own items for renting</a><br><br>
    <button type = "submit" name = "manage_items" value ="<%current_user.id%>">Manage Your Items</button><br>

  <% end %>
<% end %>
<% if (user_signed_in? && !Item.exists?(user_id: current_user.id) )%>
  <a href="/items/new" class="book_button">Add your own items for renting</a>
<% end %>

<br>

<% if (user_signed_in? && Booking.exists?(user_id: current_user.id, rent_status: true) )%>
  <%= form_tag bookings_path, method: "get" do %>
    <button type = "submit" name = "rented_items" value ="<%current_user.id%>">Items You are Renting</button>
  <% end %>
<% end %>
